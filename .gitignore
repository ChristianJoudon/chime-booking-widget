node_modules
dist
.env.local
dist-admin
dist-embed
dist-approval
server/.env
.env
FIXES_APPLIED.patch
.env.*.local
# Admin credentials, scoped away from the root so widget builds cannot inline
# them. See envDir in vite.admin.config.ts.
config/admin/.env*
# Scratch output from local asset generation.
tmp/
.DS_Store
.idea
*.iml
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Real production secrets. The .example alongside it is the template.
.env.production
backups/
